# Image Compression Coursework

Курсовой проект по сжатию изображений на языке C.

Тема проекта: реализация и сравнение методов сжатия изображений:

- SVD-сжатие;
- JPEG-подобное сжатие на основе DCT и квантования.

Также в проекте реализуются метрики качества PSNR и SSIM для сравнения исходного и восстановленного изображений.

## Что уже реализовано

На текущем этапе реализовано:

- начальная структура проекта;
- сборка через `Makefile`;
- обработка аргументов командной строки;
- чтение 24-bit BMP-файлов;
- сохранение BMP-файлов;
- хранение изображения в структуре `Image`;
- преобразование изображения в матрицу яркости;
- преобразование изображения в оттенки серого;
- расчёт MSE;
- расчёт PSNR;
- расчёт SSIM;
- разбиение изображения на блоки 8x8;
- прямое DCT для блока 8x8;
- обратное DCT для блока 8x8;
- квантование JPEG-коэффициентов;
- обратное квантование;
- JPEG-подобная обработка всего изображения;
- параметр силы квантования для JPEG-режима;
- подсчёт нулевых DCT-коэффициентов после квантования;
- примерная оценка степени сжатия.

## Структура проекта

```text
.
├── include/
│   ├── bmp.h
│   ├── jpeg.h
│   ├── matrix.h
│   ├── metrics.h
│   └── processing.h
├── src/
│   ├── bmp.c
│   ├── jpeg.c
│   ├── main.c
│   ├── matrix.c
│   ├── metrics.c
│   └── processing.c
├── images/
│   └── test_beck.bmp
├── Makefile
└── README.md
```

## Сборка

Для сборки проекта используется `make`:

```bash
make
```

Очистка скомпилированного файла:

```bash
make clean
```

## Запуск

Общий формат запуска зависит от режима работы программы.

### Grayscale

```bash
./image_compressor grayscale images/test_beck.bmp output.bmp
```

В этом режиме программа:

1. загружает BMP-изображение;
2. переводит его в оттенки серого;
3. считает PSNR и SSIM между оригиналом и grayscale-версией;
4. сохраняет результат в BMP-файл.

### Blocks

```bash
./image_compressor blocks images/test_beck.bmp output.bmp
```

В этом режиме программа:

1. загружает изображение;
2. переводит его в матрицу яркости;
3. проверяет возможность деления на блоки 8x8;
4. выводит информацию о количестве блоков;
5. показывает первый блок, DCT-коэффициенты, квантованные коэффициенты и восстановленный блок.

Этот режим используется для отладки JPEG-алгоритма.

### JPEG-like mode

```bash
./image_compressor jpeg images/test_beck.bmp output.bmp 1.0
```

Последний параметр — коэффициент квантования.

Примеры:

```bash
./image_compressor jpeg images/test_beck.bmp output_q1.bmp 1.0
./image_compressor jpeg images/test_beck.bmp output_q5.bmp 5.0
./image_compressor jpeg images/test_beck.bmp output_q10.bmp 10.0
./image_compressor jpeg images/test_beck.bmp output_q20.bmp 20.0
```

Чем больше коэффициент, тем сильнее квантование и тем больше потери качества.

Примерная логика:

```text
1.0  - стандартное квантование
5.0  - более сильные потери
10.0 - заметное ухудшение качества
20.0 - очень сильное квантование
```

## Пример вывода JPEG-режима

```text
Image compression project: SVD and JPEG
Mode: jpeg
Input file: images/test_beck.bmp
Output file: output_q1.bmp
BMP image loaded successfully
Image size: 512 x 512
JPEG quality factor: 1.00
JPEG block size: 8 x 8
Blocks by width: 64
Blocks by height: 64
Total blocks: 4096
JPEG-like processing completed
PSNR: 36.07 dB
SSIM: ...
Total DCT coefficients: 262144
Zero coefficients after quantization: 220623
Zero coefficients percent: 84.16%
Non-zero coefficients after quantization: 41521
Estimated useful coefficients percent: 15.84%
Estimated compression ratio: 6.31:1
BMP image saved successfully
```

## Метрики

### PSNR

PSNR показывает среднюю ошибку между исходным и восстановленным изображением.

Чем выше PSNR, тем лучше качество восстановления.

Измеряется в децибелах:

```text
dB
```

### SSIM

SSIM оценивает структурное сходство изображений.

Примерная интерпретация:

```text
SSIM ближе к 1.0 - изображения очень похожи
SSIM ближе к 0.0 - изображения сильно отличаются
```

В проекте используется простая глобальная версия SSIM, рассчитанная по всему изображению.

### Оценка степени сжатия

Так как результат JPEG-подобного режима сохраняется обратно в BMP, размер выходного файла не показывает настоящую степень сжатия.

Поэтому в проекте используется учебная оценка:

```text
compression ratio = total DCT coefficients / non-zero DCT coefficients
```

Чем больше нулевых коэффициентов появляется после квантования, тем меньше значимых коэффициентов остаётся и тем выше примерная степень сжатия.

## Ограничения текущей версии

Сейчас программа поддерживает только:

- BMP-файлы без сжатия;
- изображения с глубиной цвета 24 бита;
- изображения, размеры которых делятся на 8 для JPEG-режима;
- JPEG-подобное восстановление в BMP-файл, а не сохранение настоящего `.jpg`.

## План дальнейшей работы

Ближайшие этапы:

1. Начать реализацию SVD-сжатия.
2. Добавить матричные функции для SVD.
3. Реализовать SVD-режим с параметром `k`.
4. Посчитать PSNR и SSIM для SVD-результата.
5. Добавить сравнение JPEG и SVD.
6. Добавить измерение времени работы.
7. Подготовить финальный анализ результатов.
